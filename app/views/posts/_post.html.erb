<div id="<%= dom_id post %>">
  <p>
    <strong>Title:</strong>
    <%= post.title %>
  </p>
  
  <% post.post_attachments.each do |item| %>
    <%= image_tag("#{item.avatar}", {:style => "width:100px;"}) %>
  <% end %>
</div>
